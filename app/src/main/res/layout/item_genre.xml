<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools">

    <TextView
        style="@style/BaseText"
        android:id="@+id/txtGenre"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/item_genre_horizontal_margin"
        android:layout_marginTop="@dimen/item_genre_top_margin"
        android:layout_marginBottom="@dimen/item_genre_bottom_margin"
        android:background="@drawable/shape_txt_genre"
        android:textSize="@dimen/very_small_txt_size"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Какой-то жанр"/>

</androidx.constraintlayout.widget.ConstraintLayout>